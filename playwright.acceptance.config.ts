import { defineConfig, devices } from '@playwright/test';
export default defineConfig({ testDir: './acceptance', timeout: 60000, expect: { timeout: 15000 }, workers: 1, fullyParallel: false, reporter: [['list'], ['html', { outputFolder: 'acceptance-report', open: 'never' }]], use: { baseURL: process.env.ACCEPTANCE_URL || 'http://localhost:3211', trace: 'retain-on-failure', screenshot: 'only-on-failure' }, projects: [{ name: 'desktop', use: { ...devices['Desktop Chrome'], viewport: { width: 1440, height: 1000 } } }, { name: 'mobile', use: { ...devices['iPhone 13'], defaultBrowserType: 'chromium' } }] });
